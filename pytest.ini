[pytest]
# Puts the project root on sys.path so `from engine import ...` resolves without
# any IDE configuration, PYTHONPATH export, or "Mark Directory as Sources Root".
pythonpath = .
testpaths = tests
python_files = test_*.py
addopts = -v
